# ─────────────────────────────────────────────────────────────────────────────
# Eco-Formalites — Variables d'environnement
# Copiez ce fichier en ".env" et remplissez les valeurs.
# Ne commitez JAMAIS le fichier .env dans git.
# ─────────────────────────────────────────────────────────────────────────────

# Serveur
PORT=3001

# ── Email (Microsoft 365 SMTP) ──────────────────────────────────────────────
SMTP_EMAIL=ton-adresse@eco-formalites.com
SMTP_PASSWORD=ton_mot_de_passe_application
SMTP_FROM_NAME=Eco-Formalites
SMTP_HOST=smtp.office365.com
SMTP_PORT=587

# ── Google Custom Search (recherche plateforme urbanisme) ────────────────────
GOOGLE_API_KEY=AIzaSyDpWCbDnrGlTnE0IQxfePVRCrzOu2AmTuE
GOOGLE_CX=774429fd582cd4f89

# ── OpenAI (recherche GNAU / urbanisme) ──────────────────────────────────────
OPENAI_API_KEY=sk-votre_cle_openai (demander a yossi) 

# ── Securite ─────────────────────────────────────────────────────────────────
# Generez avec: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=changez_moi_en_production
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# ── Stockage fichiers (GED) ─────────────────────────────────────────────────
UPLOAD_DIR=./uploads
